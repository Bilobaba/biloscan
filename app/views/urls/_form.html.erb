<div class="container">
  <%= form_with(model: url, local: true) do |form| %>
    <% if url.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(url.errors.count, "error") %> prohibited this url from being saved:</h2>

        <ul>
        <% url.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :Url_a_surveiller %>
      <%= form.text_field :path, id: :url_path, size: 100 %>
    </div>

    <div class="field">
      <%= form.label :Commentaire %>
      <%= form.text_field :comment, id: :url_comment, size: 100 %>
    </div>

    <div class="actions btn">
      <%= form.submit %>
    </div>
  <% end %>
</div>
